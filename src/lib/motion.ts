import type { Variants } from 'motion/react'

// Shared scroll-reveal variants used across the site.
// Kept simple (opacity + small y-shift) so they read as "polish", not distraction.

export const fadeUp: Variants = {
  hidden: { opacity: 0, y: 24 },
  show: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.55, ease: [0.16, 1, 0.3, 1] },
  },
}

export const fadeIn: Variants = {
  hidden: { opacity: 0 },
  show: { opacity: 1, transition: { duration: 0.6, ease: 'easeOut' } },
}

export const scaleIn: Variants = {
  hidden: { opacity: 0, scale: 0.92 },
  show: {
    opacity: 1,
    scale: 1,
    transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] },
  },
}

/** Wrap a grid/list container with this, then give each child `variants={fadeUp}`. */
export const staggerContainer: Variants = {
  hidden: {},
  show: {
    transition: { staggerChildren: 0.1, delayChildren: 0.05 },
  },
}

/** Standard viewport config: animate once, slightly before fully in view. */
export const revealViewport = { once: true, margin: '-80px' } as const
